<h1>Listing shift types</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shift_types.each do |shift_type| %>
  <tr>
    <td><%= shift_type.title %></td>
    <td><%= shift_type.description %></td>
    <td><%= shift_type.location %></td>
    <td><%= link_to 'Show', shift_type %></td>
    <td><%= link_to 'Edit', edit_shift_type_path(shift_type) %></td>
    <td><%= link_to 'Destroy', shift_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New shift type', new_shift_type_path %>
